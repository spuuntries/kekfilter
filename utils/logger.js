/**
 * Logger function that's literally just a shorthand for console.log
 * @param {string} msg - The message to log
 * @returns {void}
 */
function logger(msg) {
  console.log(msg);
}

module.exports = logger;
